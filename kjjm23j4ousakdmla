local url = syn.request({
    Url = "https://chopperhub.kamicozii.repl.co/?key="..getgenv().key,
    Method = "GET"
})


if string.find(url.Body, getgenv().key) then
    print(getgenv().key)
else
    print("INVALID")
end
